- name: Instalação do Java
  hosts: meu_pc
  tasks:
  - name: "instala pacote wget ubuntu"
    ansible.builtin.apt:
      name: wget
      state: present
      update_cache: yes
    #when: (ansible_facts['distribution'] == "Ubuntu")